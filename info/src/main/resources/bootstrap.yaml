server:
  port: 8763
spring:
  application:
    name: info
  profiles:
    active: dev
  cloud:
    loadbalancer:
      cache:
        # 不开启缓存
        enabled: false
    #Nacos配置文件读取配置
    nacos:
      config:
        server-addr: localhost:8848
        data-id: ${spring.application.name}-${spring.profiles.active}.${nacos.config.type}
        type: yaml
        auto-refresh: true
        namespace: public
        context-path: /nacos
        file-extension: yaml
      discovery:
        server-addr: localhost:8848
        auto-register: true
        register:
          service-name: info
          ip: 127.0.0.1
          port: 8766
          metadata: { group: "public",context-path: "/nacos" }
          namespace: public



